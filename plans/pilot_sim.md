 


Next steps:
- low priority: replace cosine weighting with cell area weighting

- add map of the velocity vector field from GLORYS similar to the true temperature field one


why does the real argo ohc for the sampled cells change depending on the resolution in the plot domain time series? also in the plot_monthly_cell_maps why is the real argo ohc missing at the 1/12 level.


In the domain timeseries plot in the pilot_simulation report, can you add a third subplot that is in the middle of the two existing ones that has three lines of the total bias, sampling bias, and grid bias month over month, similar to plot_bias_vs_resolution but bias vs month. No need to check if your new code renders correctly I can check that and report back. 

Fix the observed argo anomaly tabs to be at the right scale
